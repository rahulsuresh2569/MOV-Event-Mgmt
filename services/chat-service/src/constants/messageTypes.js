// Message Types
const MESSAGE_TYPES = {
  DIRECT: 'direct',
  GROUP: 'group',
  SYSTEM: 'system',
};

// Conversation Types
const CONVERSATION_TYPES = {
  DIRECT: 'direct',
  GROUP: 'group',
};

module.exports = {
  MESSAGE_TYPES,
  CONVERSATION_TYPES,
};
